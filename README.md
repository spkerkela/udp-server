Simple client and server that talk to each other through UDP.

Multiple clients can talk to same server.

# Build

```bash
$ make all
```

# Run

```bash
$ ./build/server
# then on separate terminal
$ ./build/client
```
